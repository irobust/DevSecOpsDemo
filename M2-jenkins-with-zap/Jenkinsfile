pipeline {
  agent { 
    dockerfile {
        filename 'Dockerfile'
        dir 'M2-Jenkins-with-zap'
        label 'owasp-zap'
    }
  }
  stages {
    stage('Test') {
      steps {
        sh '''
          zap-baseline.py -t http://demo.testfire.net
        '''
      }
    }
  }
}